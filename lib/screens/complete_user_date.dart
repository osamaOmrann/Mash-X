import 'package:flutter/material.dart';

class CompleteUserData extends StatelessWidget {
  const CompleteUserData({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
